'use strict';

/**
const {
  arity,
  identity,
  constant,
  spreadArgs,
  gatherArgs,
  partArgs,
  partArgsR,
  reverseArgs,
  curry,
  uncurry,
  not,
  when,
  compose2,
  compose,
  over,
  rearg,
  sampleSize,
  sample,
  shank,
  stableSort,
  intersection,
  difference,
  union,
  uniqueElements,
  clampNumber,
  luhnCheck,
  compactWhitespace,
  pluralize,
  stringPtx,
  siPrefix,
  escapeHTML,
  unescapeHTML,
  escapeRegExp
}
*/

const fun = require('./fp_lib.js');

module.exports.default = fun;
